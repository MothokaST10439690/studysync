[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo", "php83Extensions.pdo_mysql", "php83Extensions.mysqli", "php83Extensions.mbstring"]

[start]
cmd = "php -d upload_max_filesize=100M -d post_max_size=110M -S 0.0.0.0:$PORT -t /app/public"
